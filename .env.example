# Backend API — apunta al backend-asistente local (puerto 3000)
#
# Teléfono físico con Expo Go (misma WiFi que la PC):
#   usa la IP de tu PC (el backend la imprime al arrancar)
# Emulador web / iOS Simulator:
#   http://localhost:3000/api
# Emulador Android:
#   http://10.0.2.2:3000/api
EXPO_PUBLIC_API_BASE_URL=http://192.168.x.x:3000/api

# --- Voz (producción) ---
# El usuario NO instala Whisper. Kivo usa el reconocimiento de voz del teléfono
# (gratis, viene en iOS/Android). Solo se envía TEXTO al backend.
# Requiere development build o build de tienda (no funciona en Expo Go).

# --- Voz (solo desarrollo opcional) ---
# Si pruebas en Expo Go sin build nativo, puedes apuntar a un Whisper remoto:
# EXPO_PUBLIC_WHISPER_API_URL=http://192.168.x.x:8080/inference
# EXPO_PUBLIC_WHISPER_LANGUAGE=es

# Assistant AI endpoint (POST JSON, see src/types/api.ts)
# EXPO_PUBLIC_ASSISTANT_API_URL=https://your-api.com/assistant/chat
